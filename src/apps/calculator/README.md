# Calculator

A fully-styled calculator with keyboard input support. Handles divide-by-zero and invalid input gracefully with visible error states.

**Key concepts:** Expression parsing, error handling, keyboard event listeners.
