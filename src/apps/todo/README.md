# To-Do List

A task manager with add, edit, delete, and complete actions. Tasks persist across page refreshes via localStorage. Features optional due dates and status filters (all/active/completed).

**Key concepts:** CRUD operations, localStorage persistence, list filtering.
