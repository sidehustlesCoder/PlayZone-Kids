# Data Fetcher — CodeArcade

Client-side data fetching using Python's `pyfetch` running in-browser via Pyodide (WASM).

**Key concepts demonstrated:** `pyodide.http.pyfetch` for browser HTTP requests, async/await in Python, JSON parsing, CORS considerations, allowlist-based API access — all without any server.
